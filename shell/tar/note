1.将文件index.html文件压缩为index.tar.gz
    tar -czvf index.tar.gz index.html

    其中c代表创建一个新的备份文件，
    z代表通过gzip指令处理备份文件，
    v代表显示指令执行过程，
    f代表指定备份文件。

  有一个admin的文件夹，需要将文件夹进行备份为admin.tar.gz。需要执行的命令是：
    tar -czvf admin.tar.gz admin/

2.显示压缩文件内容
    tar -tzvf test.tar.gz

    -t/--list 列出备份文件的内容。

3.已有一个index.tar.gz的文件，我们将其解压，执行的命令是：
    tar -xzvf index.tar.gz

    -z：有gzip属性,即需要用 gzip 压缩  
    -j：有bz2属性,即需要用 bzip2 压缩  


# tar.gz
    tar -czvf archive.tar.gz /path/to/dir
# tar.bz2
    tar -cjvf archive.tar.bz2 /path/to/dir
# tar.xz
    tar -cJvf archive.tar.xz /path/to/dir

# tar.gz
    tar -xzvf archive.tar.gz -C /target/path
# tar.bz2
    tar -xjvf archive.tar.bz2 -C /target/path
# tar.xz
    tar -xJvf archive.tar.xz -C /target/path
格式        压缩比      速度    适用场景
tar         1:1         最快    仅需打包，无需压缩
tar.gz      2:1~3:1     快      日常文件压缩（平衡速度与大小）
tar.bz2     3:1~4:1     中      高压缩率需求（如备份）
tar.xz      4:1+        慢      大文件或长期存储
